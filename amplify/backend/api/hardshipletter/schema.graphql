# This "input" configures a global authorization rule to enable public access to
# all models in this schema. Learn more about authorization rules here: https://docs.amplify.aws/cli/graphql/authorization-rules
input AMPLIFY {
  globalAuthRule: AuthRule = { allow: public }
} # FOR TESTING ONLY!
type Users @model {
  id: ID!
  email: String! @index
  tokens: Int
}
type History @model {
  id: ID!
  email: String! @index
  ref: String!
}
type WriterPrompt {
  msg: String @function(name: "hardshipletteraskai-dev")
}

type Query {
  getHardshipLetter: WriterPrompt
}

schema {
  query: Query
}
